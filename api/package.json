{
  "name": "ghost-sphere-core",
  "version": "0.0.1",
  "description": "Ghost Sphere Core — API gateway that unifies and proxies the Remnawave VPN ecosystem. Developed by Ghost OS.",
  "author": "Ghost OS",
  "license": "AGPL-3.0-only",
  "private": true,
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "start": "node dist/main.js",
    "start:dev": "ts-node-dev --respawn --transpile-only src/main.ts",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@nestjs/common": "^11.0.0",
    "@nestjs/config": "^4.0.0",
    "@nestjs/core": "^11.0.0",
    "@nestjs/platform-express": "^11.0.0",
    "axios": "^1.7.7",
    "reflect-metadata": "^0.2.2",
    "rxjs": "^7.8.1"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "@types/node": "^22.8.0",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.6.3"
  }
}
